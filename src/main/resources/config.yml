# Selected config
#   THIS     - use only this file for everything, including check tuning
#   ADVANCED - (GoblinMC default) use checks.yml for check tuning (physics/timer/
#              heuristics/thresholds/etc.), auto-generated on first run. Prefix,
#              messages and kick/ban commands are still edited right here in
#              config.yml and are re-applied to checks.yml automatically.
#   ONLINE/[config-name] (planned) - use the cloud config
config: ADVANCED

prefix: "&8[&c&lIntave&8]&7 "

kick-commands:
  - "intave internals kickip {player} &cYou have been kicked, as an account with your ip address was removed from the game"
  - "kick {player} &cYou have been removed from the game based on data that was collected by the anti-cheat ({log-id})"

ban-commands:
  - "intave internals kickip {player} &cYou have been kicked, as an account with your ip address was removed from the game"
  - "ban {player} Cheating / {log-id}"

# Cloud token
#   Required for cloud features, such as cloud-based detection, logging and cross-account analysis.
#   Obtainable at https://intave.de
cloud-token: "ct_EXAMPLE-TOKEN"

# Movement setback sensitivity
#   Choose between AGGRESSIVE, CAREFUL, LENIENT, BARELY and SILENT
#   FALSE POSITIVES? -> REPORT THEM
physics-mm: BARELY

# Rejoin delay after issued kick (in seconds)
rejoin-delay: 3600

# Cloud privacy control
#   HIGHEST_PRIVACY - only the most critical data is sent (player UUID and usage data)
#   BEST_DETECTION  - also use IP address for cross-account analysis & detection
privacy: BEST_DETECTION

verbose: "{prefix} &c{player}&7/{trust-color}&7 {message} {details} (+{vladded} -> {vl})"
notify: "{prefix} &c&lInfo&8: &7{text}"